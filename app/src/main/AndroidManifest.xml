<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.find.find_food">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Find_Food"
        android:usesCleartextTraffic="true">
        <activity android:name=".Confirmation.User_Details_Activity"></activity>
        <activity android:name=".Confirmation.Confirmation_Activity" />
        <activity android:name=".Add_Item.Item_Add_Activity" />
        <activity android:name=".Add_Item.Manager_Activity" />
        <activity android:name=".Home_Page_Activity" />
        <activity android:name=".User_Registration_Activity" />
        <activity android:name=".Splash_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>